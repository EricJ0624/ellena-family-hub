/**
 * 위젯 사용성 임계값 (Phase 5 돋보기 모드)
 *
 * 실측 픽셀 너비/높이가 아래 값 미만이면 WidgetChrome이 🔍 버튼을 노출한다.
 * ResizeObserver의 contentRect 기준.
 */
export const USABILITY_MIN_W_PX = 160;
export const USABILITY_MIN_H_PX = 120;
